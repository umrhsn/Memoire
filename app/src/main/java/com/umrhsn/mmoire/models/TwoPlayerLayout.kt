package com.umrhsn.mmoire.models

enum class TwoPlayerLayout {
    FACE_TO_FACE,  // Players at short ends (Left/Right split)
    SIDE_BY_SIDE,  // Players at one long side (Vertical split, same orientation)
    OPPOSITE       // Players at long sides (Top/Bottom split)
}
