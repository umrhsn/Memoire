<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/llMemoryCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical">

    <androidx.cardview.widget.CardView
        android:id="@+id/cvMemoryCard"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_margin="8dp"
        android:elevation="10dp"
        app:cardCornerRadius="3dp">

        <!-- TODO: add attribution to the image, https://www.flaticon.com/free-icons/memory-game Memory game icons created by Freepik - Flaticon -->
        <ImageButton
            android:id="@+id/ibMemoryCard"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/white"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/memory_card_facedown" />
    </androidx.cardview.widget.CardView>
</LinearLayout>