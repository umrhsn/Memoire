<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Mémoire" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryVariant">@color/purple</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="backgroundColor">@color/light_purple</item>
        <!--   Default Text color    -->
        <item name="android:textColor">@color/black</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Action bar style. -->

        <!-- Status bar color. -->
        <!-- TODO: make it compatible with api level < 23 -->
        <!--    current min = 21 -->
        <!--        <item name="android:statusBarColor">?attr/colorPrimary</item>-->
        <!--        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>-->

        <!-- Navigation bar color. -->
        <!-- TODO: make it compatible with api level < 27 -->
        <!--    current min = 21 -->
        <!--        <item name="android:navigationBarColor">?attr/colorPrimary</item>-->
        <!--        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>-->

        <!-- Customize your theme here. -->
        <item name="android:windowBackground">?attr/backgroundColor</item>
    </style>

    <style name="DotsDarkTheme" parent="@style/Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@color/white</item>
    </style>

    <!--  CardView style  -->
    <style name="MyCardViewStyle" parent="CardView">
        <!-- Card background color -->
        <item name="cardBackgroundColor">@android:color/white</item>
        <!-- Ripple for API 21 of android, and regular selector on older -->
        <item name="android:foreground">?android:selectableItemBackground</item>
        <!-- Resting Elevation from Material guidelines -->
        <item name="cardElevation">5dp</item>
        <!-- Add corner Radius -->
        <item name="cardCornerRadius">5dp</item>
        <item name="android:layout_margin">5dp</item>
    </style>

    <!-- This will set the fade in animation on all your activities by default -->
    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>
</resources>